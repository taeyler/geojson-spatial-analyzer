apiVersion: v1
kind: Service
metadata:
  name: geojson-analyzer-service
spec:
  selector:
    app: geojson-analyzer
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP